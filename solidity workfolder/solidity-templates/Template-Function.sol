pragma solidity >= 0.7.0 < 0.9.0;

//contract "contract name" {

    function "function name" ("type of data (ex: uint)" "variable name (ex: _nr)", "second parameter", "etc.")
     "keywords (ex:public,view,returns(\"type of data to return\")" {

    }

//}